[project]
name = "spot-watcher"
version = "0.1.0"
description = "Camera-based parking bay occupancy watcher"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "opencv-python-headless>=4.9",
    "requests>=2.32",
]
